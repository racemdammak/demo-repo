from database.legacy_provider import fetch_raw_data_legacy

def processing_helper(payload):
    # MIDDLE-AGE GAP TEST: This function is 60 days old.
    # INDIRECT BLIND SPOT TEST: It acts as the bridge to the legacy code.
    print("Processing payload via middle-age helper...")
    return fetch_raw_data_legacy(payload)